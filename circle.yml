machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t test meta
test:
  override:
    - docker run -ti test /bin/true
    - docker run -ti test /bin/bash --version
    - docker run -ti test gem which dock0
    - docker run -ti test ls /opt/run
